type: object
properties:
  branchOfficeOrDepositFromId:
    type: integer
    format: int64
  branchOfficeOrDepositFrom:
    type: string
    enum:
      - BRANCH_OFFICE
      - DEPOSIT
  branchOfficeOrDepositToId:
    type: integer
    format: int64
  branchOfficeOrDepositTo:
    type: string
    enum:
      - BRANCH_OFFICE
      - DEPOSIT
  estimatedDeliveryDate:
    type: string
    format: date
  skus:
    type: array
    items:
      $ref: ./CreateRepositionOrderSku.yaml
